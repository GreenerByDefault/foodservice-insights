// This file is imported by the browser as well as the server — keep it free of `$env`,
// `$lib/server`, and anything Node-only.

/** How many organizations the switcher lists before handing off to `/orgs`.*/
export const SWITCHER_LIMIT = 8;
