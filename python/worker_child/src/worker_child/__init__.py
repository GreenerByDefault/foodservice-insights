"""One analysis run, spawned as a child process by the TypeScript worker parent.

Empty for now. It touches neither the database nor the blob store; it reads and writes the
run directory its parent sets up. See ARCHITECTURE.md § Worker.
"""
